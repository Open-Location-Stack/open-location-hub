[project]
name = "open-rtls-hub-scripts"
version = "0.1.0"
description = "General-purpose Python utility scripts for Open RTLS Hub demos"
requires-python = ">=3.12"
dependencies = [
  "requests>=2.33.0",
  "websocket-client>=1.9.0",
]

[tool.uv]
package = false
